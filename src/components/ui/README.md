# UI primitives

This directory is managed through the shadcn CLI and then mapped to the project token contract. It owns the retained Button, Card, Badge, Separator, AlertDialog, Skeleton, Sonner, Input, and Label primitives. Application composition belongs in `components/system` or `components/shell`, not in these primitives.
