# Reviewed migrations

Drizzle Kit writes generated, reviewable SQL and snapshots here. Migration `0000_tidy_dagger` establishes Better Auth and workspace ownership. Migration `0001_melodic_bloodstrike` adds the workspace-scoped `tasks` table. Migration `0002_even_moonstone` adds Gates, Labels, Quest-label relationships, and Gate assignment. Migration `0003_abandoned_gamora` adds composite tenant foreign keys plus `pg_trgm` search indexes. Migration `0004_romantic_the_liberteens` adds shared Better Auth rate-limit buckets. Migration `0005_slippery_exiles` finalizes recurring Quest fields and adds user settings, reminder schedules, idempotent deliveries, and in-app notifications. Migration `0006_serious_proteus` adds the nullable workspace-scoped offline mutation UUID and unique partial index used for idempotent replay. Migration `0007_friendly_invisible_woman` adds immutable activity events, an append-only XP ledger, and the rebuildable per-user progression projection. Migration `0008_aberrant_liz_osborn` adds the workspace- and Quest-scoped attachment metadata lifecycle. Production must use `pnpm db:migrate`; schema push is prohibited.
